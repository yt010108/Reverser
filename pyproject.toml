[project]
name = "hermes-ctf-harness"
version = "0.1.0"
description = "Pi-controlled isolated CTF reversing harness with time-gated research"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ctf-harness = "ctf_harness.cli:main"

[tool.setuptools]
package-dir = {"" = "code"}

[tool.setuptools.packages.find]
where = ["code"]

[tool.unittest]
start-directory = "tests"
